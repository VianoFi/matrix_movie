@page
@model LoginModel
@{
    ViewData["Title"] = "Login";
}

<link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&display=swap" rel="stylesheet">

<div class="container login-container">
    <div class="matrix-card-login mx-auto mt-5 p-5 text-center">
        <h1 class="matrix-title mb-4">LOGIN</h1>

        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3 text-start">
                <label asp-for="Input.Email" class="form-label">Email</label>
                <input asp-for="Input.Email" class="form-control matrix-input" autocomplete="username" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>

            <div class="mb-3 text-start">
                <label asp-for="Input.Password" class="form-label">Password</label>
                <input asp-for="Input.Password" class="form-control matrix-input" autocomplete="current-password" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>

            <div class="form-check mb-3 text-start">
                <input asp-for="Input.RememberMe" class="form-check-input matrix-check" />
                <label asp-for="Input.RememberMe" class="form-check-label">Ricordami</label>
            </div>

            <button type="submit" class="btn-matrix w-100">ACCEDI</button>

            <div class="mt-4">
                <a asp-page="./Register" class="matrix-link">Registrati</a> |
                <a asp-page="./ResetPassword" class="matrix-link">Password dimenticata?</a>
            </div>
        </form>
    </div>
</div>

@section Styles {
    <style>
        body {
            background: #000;
            color: #39FF14;
            font-family: 'Share Tech Mono', monospace;
        }

        .matrix-card-login {
            background: #0d0d0d;
            border: 1px solid #39FF14;
            border-radius: 10px;
            box-shadow: 0 0 15px #39FF14;
            max-width: 450px;
        }

        .matrix-title {
            color: #39FF14;
            text-shadow: 0 0 10px #39FF14;
        }

        .matrix-input {
            background-color: #000;
            color: #39FF14;
            border: 1px solid #39FF14;
            box-shadow: none;
        }

            .matrix-input:focus {
                background-color: #000;
                color: #39FF14;
                border-color: #00ff88;
                box-shadow: 0 0 10px #39FF14;
            }

        .matrix-check {
            background-color: #000;
            border: 1px solid #39FF14;
        }

        .btn-matrix {
            border: 1px solid #39FF14;
            color: #39FF14;
            background: transparent;
            padding: 10px 22px;
            border-radius: 8px;
            font-weight: bold;
            text-transform: uppercase;
            transition: all .3s ease;
            text-decoration: none;
        }

            .btn-matrix:hover {
                background: #39FF14;
                color: #000;
                box-shadow: 0 0 12px #39FF14;
            }

        .matrix-link {
            color: #39FF14;
            text-decoration: none;
            text-shadow: 0 0 8px #39FF14;
        }

            .matrix-link:hover {
                color: #000;
                background: #39FF14;
                padding: 3px 8px;
                border-radius: 5px;
            }
    </style>
}
